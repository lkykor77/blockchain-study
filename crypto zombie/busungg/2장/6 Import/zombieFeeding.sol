pragma solidity ^0.4.19;

import "./zombiefactory.sol";

contract ZombieFeeding is ZombieFactory {
    
}

/**
다수의 파일이 있는 구조를 갖추었으니 
import를 활용하여 다른 파일의 내용을 읽어올 필요가 있네.

새로운 파일 zombiefeeding.sol에 
zombiefactory.sol를 불러 온다(import).
 */